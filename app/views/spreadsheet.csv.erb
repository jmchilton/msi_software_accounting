<% require 'faster_csv' %>
<%= @fields.map{ |field| field[:label] }.join(",") %>
<%= FasterCSV.generate do |csv|
    @rows.each do |row|
      csv << @fields.map { |field| row[field[:field]]}
    end
end %>