<%= model_field('count', "Number of Recorded Executions", @summary[:count]) %>
<% if @summary[:count] > 0 %>
  <%= model_field('first', "Date of Oldest Recorded Execution", @summary[:first]) %>
  <%= model_field('last', "Date of Latest Recorded Execution", @summary[:last]) %>
<% end %>