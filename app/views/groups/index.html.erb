<h1>Listing groups</h1>

<table>
  <tr>
    <th>Gid</th>
    <th>Name</th>
    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.gid %></td>
    <td><%= group.name %></td>
    <td><%= link_to 'Show', group %></td>
  </tr>
<% end %>
</table>

<br />

