<h1>Collectl Executable </h1> <%= @collectl_executable.name %></h1>
<%= model_field('name', "Name", @collectl_executable.identifier) %>
<%= model_field('resource', "Resource", @colleclt_executable.resource.blank? ? "Not mapped to a resource" : @collectl_executable.resource.name) %>

<%= model_link "Back to Resource #{@collectl_executable.resource.name}", resource_path(@collectl_executable.resource) %>
