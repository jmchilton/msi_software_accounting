<h1>Listing event_types</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Feature</th>
    <th>Vendor</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @event_types.each do |event_type| %>
  <tr>
    <td><%= event_type.id %></td>
    <td><%= event_type.feature %></td>
    <td><%= event_type.vendor %></td>
    <td><%= link_to 'Show', event_type_path(event_type) %></td>
    <td><%= link_to 'Edit', edit_event_type_path(event_type) %></td>
  </tr>
<% end %>
</table>

<br />
